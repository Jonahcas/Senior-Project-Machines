Machine:
Xaphania - Double service vulnerability. Grab a file from FTP, decode it for possible SMB logins. Use login to access "$C" share.
        Once in share, navigate through the file system and start deleting everything.